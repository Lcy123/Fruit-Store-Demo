<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/eva_image"
        android:layout_width="match_parent"
        android:layout_height="220dp" />
    <TextView
        android:id="@+id/eva_source"
        android:text="source"
        android:textColor="#ffffff"
        android:layout_alignTop="@+id/eva_image"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/eva_title"
        android:text="title"
        android:textColor="#ffffff"
        android:layout_alignTop="@+id/eva_image"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/eva_tail"
        android:text="tail"
        android:layout_alignBottom="@+id/eva_image"
        android:layout_centerHorizontal="true"
        android:drawableLeft="@mipmap/ic_watch"
        android:layout_marginBottom="20dp"
        android:textColor="#ffffff"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</RelativeLayout>